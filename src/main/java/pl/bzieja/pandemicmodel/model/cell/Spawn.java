package pl.bzieja.pandemicmodel.model.cell;

import java.awt.*;

public class Spawn extends Cell {
    public Spawn(boolean walkable, Color color, int x, int y) {
        super(walkable, color, x, y);
    }
}
